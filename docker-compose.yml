version: '3.8'
services:
  reyox:
    build: .
    image: reyox:latest
    container_name: reyox-dev
    ports:
      - "5173:5173" 
    environment:
      - HOST=0.0.0.0
    working_dir: /app
    restart: unless-stopped